import SectionHeading from '../components/SectionHeading';
export default function Process(){const steps=[['fa-comments','Understand','We listen to your business need and assess the current situation.'],['fa-compass-drafting','Design','We recommend a solution that fits your goals, environment and budget.'],['fa-screwdriver-wrench','Implement','We install, configure, develop and test the solution properly.'],['fa-headset','Support','We provide guidance and maintenance so your solution keeps working.']];return <section className="process-section"><div className="container"><SectionHeading eyebrow="How we work" title={<>From idea to <span>implementation.</span></>} /><div className="process-grid">{steps.map(([icon,title,text],index)=><><div className="process-step reveal" key={title}><span>0{index+1}</span><i className={`fas ${icon}`} /><h3>{title}</h3><p>{text}</p></div>{index<steps.length-1&&<div className="process-line" key={`${title}-line`} />}</>)}</div></div></section>}
