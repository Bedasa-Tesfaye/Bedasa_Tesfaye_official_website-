import SectionHeading from '../components/SectionHeading'; import products from '../data/products';
export default function Products(){return <section id="products" className="section products-section"><div className="container"><SectionHeading eyebrow="Technology products" title={<>Quality equipment, <span>properly matched.</span></>} text="We help you choose the right products for your space, needs and budget—then install and configure them so they work from day one." /><div className="product-grid">{products.map(product=><article className="product-card reveal" key={product.title}><div className={`product-image ${product.network?'product-network':''}`}>{product.network?<><i className="fas fa-network-wired" /><span>CONNECTED</span><small>ROUTERS · SWITCHES · CABLES</small></>:<img src={product.image} alt={product.alt} loading="lazy" />}<span className="product-type">{product.type}</span></div><div className="product-content"><div className="product-icon"><i className={`fas ${product.icon}`} /></div><h3>{product.title}</h3><p>{product.text}</p><ul>{product.items.map(item=><li key={item}>{item}</li>)}</ul><a href="#contact">Request availability <i className="fas fa-arrow-right" /></a></div></article>)}</div><div className="product-note reveal"><i className="fas fa-circle-info" /><p><strong>Need a recommendation?</strong> Tell us what you are setting up and we will prepare a practical equipment list and quote.</p><a href="#contact" className="text-link">Talk to our team <i className="fas fa-arrow-right" /></a></div></div></section>}
