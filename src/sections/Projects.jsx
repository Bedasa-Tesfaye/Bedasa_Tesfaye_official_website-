import { useState } from 'react'; import SectionHeading from '../components/SectionHeading'; import projects from '../data/projects';
export default function Projects(){const[filter,setFilter]=useState('all');const filters=[['all','All'],['software','Software'],['web','Web'],['systems','Business Systems']];return <section id="projects" className="section"><div className="container"><SectionHeading eyebrow="Selected work" title={<>Projects that turn technology into <span>results.</span></>} text="A growing portfolio across software, business systems, networking and digital experiences." /><div className="project-filters reveal">{filters.map(([key,label])=><button className={`project-filter ${filter===key?'active':''}`} onClick={()=>setFilter(key)} key={key}>{label}</button>)}</div><div className="projects-grid">{projects.filter(project=>filter==='all'||project.category.includes(filter)).map(project=><article className="project-card reveal" key={project.title}><div className={`project-visual ${project.style}`}><i className={`fas ${project.icon}`} /><span>{project.label}</span></div><div className="project-body"><div className="project-meta">{project.meta}</div><h3>{project.title}</h3><p>{project.text}</p><div className="tags">{project.tags.map(tag=><span key={tag}>{tag}</span>)}</div></div></article>)}</div></div></section>}
