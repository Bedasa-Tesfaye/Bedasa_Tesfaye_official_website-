import SectionHeading from '../components/SectionHeading'; import services from '../data/services';
export default function Services(){return <section id="services" className="section"><div className="container"><SectionHeading eyebrow="What we do" title={<>Technology services built around <span>your needs.</span></>} text="From physical infrastructure to digital platforms, we deliver solutions that solve real business problems." /><div className="services-grid">{services.map((service,index)=><article className={`service-card reveal ${service.featured?'featured':''}`} key={service.title}><div className="service-number">{String(index+1).padStart(2,'0')}</div><div className="service-icon"><i className={`fas ${service.icon}`} /></div><h3>{service.title}</h3><p>{service.text}</p><ul>{service.items.map(item=><li key={item}>{item}</li>)}</ul><a href="#contact">{service.link} <i className="fas fa-arrow-right" /></a></article>)}</div></div></section>}
