import { useEffect, useState } from 'react';
const links = [['Home','#home'],['Services','#services'],['Products','#products'],['Solutions','#solutions'],['Projects','#projects'],['About','#about'],['Contact','#contact']];
export default function Navbar() { const [menuOpen,setMenuOpen]=useState(false); const [theme,setTheme]=useState('dark'); useEffect(()=>setTheme(localStorage.getItem('michu-theme')||'dark'),[]); const toggleTheme=()=>{const next=theme==='dark'?'light':'dark';setTheme(next);document.documentElement.dataset.theme=next;localStorage.setItem('michu-theme',next)}; return <header id="header"><nav className="navbar container"><a className="brand" href="#home"><span className="brand-icon"><i className="fas fa-microchip" /></span><span><strong>Michu</strong><small>Technology Solutions</small></span></a><ul className={`nav-links ${menuOpen?'open':''}`}>{links.map(([label,href])=><li key={href}><a href={href} onClick={()=>setMenuOpen(false)}>{label}</a></li>)}</ul><div className="nav-actions"><button className="icon-btn" onClick={toggleTheme} aria-label="Toggle theme"><i className={`fas fa-${theme==='dark'?'sun':'moon'}`} /></button><a className="nav-cta" href="#contact">Request a Service <i className="fas fa-arrow-right" /></a><button className={`hamburger ${menuOpen?'active':''}`} onClick={()=>setMenuOpen(!menuOpen)} aria-label="Open menu" aria-expanded={menuOpen}><span /><span /><span /></button></div></nav></header>; }
